<Page
    x:Class="_22_Style.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:_22_Style"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Page.Resources>
        <Style TargetType="Button" >
            <Setter Property="Control.FontFamily" Value="Times New Romana" />
            <Setter Property="Control.Background" Value="Azure" />
            <Setter Property="Control.Padding" Value="10" />
            <Setter Property="Control.Margin" Value="20" />
        </Style>
    </Page.Resources>

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel>
            <Button Content="Boton sin Estilo" Style="{x:Null}" />
            <Button Content="Boton con estilo" />
            <Button Content="Otro con estilo" />
        </StackPanel>
    </Grid>
</Page>
