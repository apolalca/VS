<Page
    x:Class="BindingConElementos.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BindingConElementos"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel>
            <Slider Minimum="1" Maximum="101" Value="1" StepFrequency="1" TickFrequency="1" Name="sld" TickPlacement="Outside"></Slider>
            <TextBlock Text="Sample Text" FontSize="{Binding ElementName=sld, Path=Value}"></TextBlock>
        </StackPanel>
        <StackPanel VerticalAlignment="Bottom">
            <TextBox Text="{Binding ElementName=sld, Path=Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" InputScope="Number" KeyDown="txtBox_KeyDown" Name="txtBox"></TextBox>
        </StackPanel>
    </Grid>
</Page>
